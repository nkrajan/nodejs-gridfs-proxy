default:
    listen:
        host:   127.0.0.1
        port:   7890

    gridfs:
        host:               localhost
        port:               27017
        auto_reconnect:     true
        database:           pixlish_gridfs

    debug: false